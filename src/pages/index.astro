---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
    <style>
      :root{
        --bg:#0b0f1a; --ink:#f5f6ff; --muted:#b9c3e0; --accent:#a7e0ff; --accent2:#ffd166;
        --panel:#11162a; --edge:#222b4f; --glow: 0 0 24px rgba(167,224,255,.35);
      }
      html,body{height:100%}
      body{
        margin:0; font:16px/1.6 ui-rounded, system-ui, Segoe UI, Roboto, Ubuntu;
        color:var(--ink);
        background:
          url('/Mountains.png') no-repeat center center fixed,
          radial-gradient(1000px 600px at 15% -10%, rgba(21,34,75,0.8) 0%, var(--bg) 60%),
          radial-gradient(900px 700px at 90% 10%, rgba(27,34,65,0.8) 0%, transparent 60%),
          linear-gradient(180deg,rgba(11,15,26,0.8),rgba(11,15,26,0.8));
        background-size: cover;
      }
      header.site{
        position:sticky;top:0;z-index:5;
        background-color: rgba(255,255,255,0.88);
        color: #000;
        backdrop-filter: blur(6px);
        border-bottom: 1px solid rgba(0,0,0,.08);
      }
      main{max-width:1100px;margin:0 auto;padding:28px}

      .hero{
        margin-top:12px; display:grid; gap:26px; grid-template-columns:1.2fr .8fr; align-items:center;
      }
      @media (max-width:920px){ .hero{grid-template-columns:1fr} }
      .crest{display:flex;align-items:center;gap:12px; color:#000}
      .crest .gem{width:18px;height:18px;border-radius:50%;background:var(--accent);box-shadow:var(--glow)}
      .title{margin:0;font-size:42px;line-height:1.05;letter-spacing:.4px;color:#000}
      .subtitle{margin:.3rem 0 0;color:#222}
      .badges{display:flex;flex-wrap:wrap;gap:10px;margin-top:10px}
      .pill{padding:8px 10px;border-radius:999px;border:1px dashed #2a3b76;background:#0f1736;color:#d7e5ff;font-size:12px}

      .scroll{background:linear-gradient(180deg,#111833,#0e142b); border:1px solid var(--edge); border-radius:18px; padding:20px; box-shadow:0 18px 60px rgba(0,0,0,.35)}
      .sparkles{position:relative}
      .sparkles:after{content:""; position:absolute; inset:-2px; pointer-events:none; border-radius:20px;
        background: radial-gradient(120px 60px at 10% 10%, rgba(255,209,102,.18), transparent 60%),
                    radial-gradient(160px 70px at 90% 30%, rgba(167,224,255,.18), transparent 60%);
        filter: blur(2px);
      }

      /* panels below */
      .section{margin:32px 0}
      .section h2{margin:.2rem 0 .6rem;font-size:28px;color:#fff}
      .grid{display:grid;gap:14px}
      .card{background:linear-gradient(180deg,#101632,#0c122a);border:1px solid var(--edge);border-radius:16px;padding:16px;color:var(--ink);box-shadow:0 12px 40px rgba(0,0,0,.35)}
      .card h3{margin:.2rem 0 .2rem}
      .card p{margin:.2rem 0 0;color:var(--muted)}
    </style>
  </head>
  <body>
    <header class="site"><Header /></header>
    <main>
      <section class="hero">
        <div>
          <div class="crest"><span class="gem" aria-hidden="true"></span><span>DM Wasem welcomes you</span></div>
          <h1 class="title">Adventurers, welcome to the Table</h1>
          <p class="subtitle">Campaign portals, session notes, and worlds—explore at your leisure.</p>
          <div class="badges">
            <span class="pill">World: Acroterra</span>
            <span class="pill">Fantasy Setting</span>
          </div>
        </div>

        <div class="scroll sparkles">
          <h3 style="margin:.2rem 0 .4rem">About Acroterra</h3>
          <p style="margin:.2rem 0 .6rem;color:var(--muted)">
            An ancient world shaped by rival deities, scarred by the War of Eternal Night, and alive with magic, dragons, and restless undead. Today, the welcoming realm of Zoria, the tense lands of Avrela, and war‑torn Neucrela each chart their own fate while legends—old and new—stir.
          </p>
          <ul style="margin:0 0 0 1rem; padding:0; color:var(--muted)">
            <li><strong>Zoria:</strong> a cultural crossroads scarred by war, famed for the floating Tangled Forest and Lake Zoria.</li>
            <li><strong>Neucrela:</strong> human empires, silver‑starved wastes haunted by undead, and alliances against tyranny.</li>
            <li><strong>Avrela:</strong> god‑touched coasts, storm‑blessed isles, and cities divided by old tensions.</li>
          </ul>
        </div>
      </section>

      <!-- Restored panels -->
      <section class="section">
        <h2>Worlds & One‑Shots</h2>
        <div class="grid" style="grid-template-columns:repeat(auto-fit,minmax(240px,1fr))">
          <article class="card">
            <h3>Acroterra</h3>
            <p>Deity‑forged lands, dragon heirs, scarce silver, and whispers of another war—your first step begins here.</p>
          </article>
          <article class="card">
            <h3>Coming Soon</h3>
            <p>New realms appear as the weave shivers. Watch this space for fresh adventures.</p>
          </article>
        </div>
      </section>

      <section class="section">
        <h2>Table Rules</h2>
        <div class="card">
          <ul style="margin:0 0 0 1rem;color:var(--muted)">
            <li>Be excellent to each other.</li>
            <li>Have fun, lift your party, share the spotlight.</li>
            <li>Safety tools welcome—speak up and we’ll adjust.</li>
          </ul>
        </div>
      </section>
    </main>
    <Footer />
  </body>
</html>
